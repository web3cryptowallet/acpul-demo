### 19100 DOTA::camera

# - camera icon graphics
# - camera frame
# - camera view

draw(r0, 19101);

### 19101 DOTA::camera.draw

color(u0, 1,1,0,1);
rect(u0, 6, 0,0,w,h);

#circle(u0, 6, px+p.point.start.x, py+p.point.start.y, p.radius, 0, 30, 0);
