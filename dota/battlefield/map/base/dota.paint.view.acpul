### dota.paint.view
# pool,id,i

pool0 p0;
id0 p1;
i0 p2;

_ @dota.node.base0;

_ @paint.inc;

#_ @photoneo.common.gl.extra;

#draw.fid(dota.building.crunch1.draw);

paint0 0;
sn0 0;

if ((paint0==0)*w*h>0) {
 new.paint.view(paint0, 0,0,1, w,h, 20000,0,610); # NY2025
};

