### dota.space.building.container

_ @easy.start.new1;

sn0 0;
container0 0;

new.container {
 new.ui.shader.node3(container0,  0,0,1, 200,200, 70014,28993); # dota.building.container
};

if ((base0==0)*w*h>0) {
 new.container;
};

# COMMON [

# Optimize: draw once 
frame0 0;
frame0+=1;
if (frame0==4) { # magic number 4
# node.value.s(u0, container0,1, 1); # draw.off
};

draw.fid(dota.building.crunch1.draw);

x00 r2;
y00 r3;

x00:=node.x(sn0);
y00:=node.y(sn0);

# COMMON ]


