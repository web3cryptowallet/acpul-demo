### minimap.viewport

w:=100;
h:=100;

_ @help.inc1;

draw.fid(minimap.viewport.draw);

if (r0) {
 x:=rnd(u0)*100;
};

### minimap.viewport.draw

color(u0, 1,1,1,1);
rect(u0, 2, 0,0, w,h);
color(u0, 0.5,0.5,0,0.5);
rect(u0, 2, 0+2,0+2, w-4,h-4);

